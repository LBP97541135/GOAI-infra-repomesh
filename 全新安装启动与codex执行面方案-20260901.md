# RepoMesh 全新安装·启动·codex 中转站执行面 —— 分阶段执行方案

> 版本：v2（2026-09-01）
> 修正点：① codex CLI 已安装，不再重复安装；② 模型配置不使用 deepseek（那是 `.env.example` 模板示例值，已从本方案清除）；③ codex 接入改为**中转站**（OpenAI 兼容），信息届时向用户索取；④ 改为**分阶段执行，每阶段完成向用户确认后再进入下一阶段**。
> 依据：仓库 main 最新代码（`960d1b0`）全新扫描 + 实测（`alembic heads` 双 head 已确认）。

---

## 阶段总览

```
阶段 0  信息收集（两次索取：阶段2 要平台模型配置；阶段3 要中转站信息）
   ↓ 确认
阶段 1  清空旧数据 + 修复代码缺陷（双 head、postgres restart）   ← 含破坏性清空，执行前单独确认
   ↓ 确认
阶段 2  全新启动完整平台 + 初始化管理员
   ↓ 确认
阶段 3  codex 中转站接入 + 本地执行面就绪（6 成员 readiness 全绿）
   ↓ 确认
阶段 4  端到端验收（重启恢复演练）+ 提交代码修复
```

---

## 阶段 0：需要你提供的信息（到对应阶段我会来要）

| 用途 | 信息 | 何时要 |
|---|---|---|
| 平台侧模型（阶段2 .env） | `REPOMESH_MODEL_API_KEY` / `REPOMESH_MODEL_BASE_URL` / `REPOMESH_MODEL`（规划+AgentTeams 共用；是否复用中转站？） | 阶段 2 开始前 |
| codex 中转站（阶段3） | ① base_url（如 `https://api.xxx.com/v1`）② API Key ③ 模型名 ④ 协议：`chat`（/chat/completions）还是 `responses`（/responses） | 阶段 3 开始前 |
| codex 版本 | 已安装，执行时确认 `codex --version` | 阶段 3 验证 |

> codex CLI 已由你安装，本方案不再包含任何安装 codex 的步骤；只在 3.1 做连通性验证时确认版本与登录状态。

---

## 阶段 1：清空旧数据 + 修复代码缺陷

> ⚠️ 本阶段 1.1 是**破坏性**操作（删除数据卷与本地状态）。执行前我会再次和你确认清单。

### 1.1 彻底清空（按"全新用户"标准）

```powershell
# 停栈
docker compose down
docker compose --profile console down
docker compose --profile platform down
# 清 AgentTeams 执行面容器（若存在）
docker stop agentteams-manager agentteams-controller 2>$null
docker rm -f agentteams-manager agentteams-controller 2>$null
docker ps -a --format "{{.Names}}" | Select-String "^agentteams-worker-" | ForEach-Object { docker rm -f $_ 2>$null }
# 删数据卷（数据库/对象存储全清；先 inspect 确认未被其他项目共用）
docker volume inspect repomesh-postgres agentteams-data repomesh-console-postgres
docker volume rm repomesh-postgres repomesh-console-postgres 2>$null
docker volume rm agentteams-data 2>$null
# 删仓库内 gitignored 本地状态
Remove-Item -Recurse -Force .secrets, output, .repomesh-dev, .repomesh-workspaces, .repomesh-e1 -ErrorAction SilentlyContinue
# 清除旧 .env 中的脏配置（不要 deepseek 示例值）
```

### 1.2 修复迁移双 head（当前 main 的缺陷，空库 `upgrade head` 必失败）

```powershell
# 用 merge 迁移把两个 head 收敛成一个
.\.venv\Scripts\python.exe -m alembic merge -m "merge decision-chain and operational-responses heads" 41c1e6609ea6 20260831_0051
# 确认只剩一个 head
.\.venv\Scripts\python.exe -m alembic heads   # 期望仅一行
# （可选）空库实测迁移
docker run --rm -d --name repomesh-mig-check -e POSTGRES_PASSWORD=x -e POSTGRES_DB=repomesh -p 15433:5432 postgres:17-alpine
$env:REPOMESH_DATABASE_URL="postgresql+asyncpg://postgres:x@localhost:15433/repomesh"
.\.venv\Scripts\python.exe -m alembic upgrade head
docker stop repomesh-mig-check
```

### 1.3 postgres 补 restart 策略（落地同事建议）

`compose.yaml` 的 `postgres` 服务补 `restart: unless-stopped`（与 api/web/bootstrap 一致）。

### 1.4 阶段产出与验证

- [ ] `alembic heads` 只剩一个 head
- [ ] `compose.yaml` postgres 有 `restart: unless-stopped`
- [ ] 旧容器/卷/本地状态已清空（`docker ps -a`、`docker volume ls`、目录检查）

**⏸ 确认点 1 → 你确认后进入阶段 2**

---

## 阶段 2：全新启动完整平台

### 2.1 准备 `.env`（不含 deepseek 示例）

从 `.env.example` 复制，模型配置项用**你提供的信息**（阶段 0 索取），其余保持默认：
```dotenv
REPOMESH_MODEL_API_KEY=（你提供）
REPOMESH_MODEL_BASE_URL=（你提供）
REPOMESH_MODEL=（你提供）
```

### 2.2 首次启动（含 AgentTeams 执行面）

```powershell
.\scripts\start-platform.ps1 -InstallAgentTeams
```
脚本自动：起 postgres（自动迁移）→ 安装/检查 AgentTeams → 注入 controller/Matrix/MinIO 凭据 → 生成 `.secrets/` → 起 api → web → bootstrap → 就绪检查。

### 2.3 就绪检查 + 初始化管理员

```powershell
Invoke-WebRequest http://127.0.0.1:8000/health/ready
Invoke-WebRequest http://127.0.0.1:5280/
docker compose --profile platform ps
```
全新空库无账号：控制台首次访问"初始化管理员"（密码 ≥12 位）→ 重新登录。

### 2.4 阶段产出与验证

- [ ] `docker compose --profile platform ps`：postgres/api/web/bootstrap 全部 Up + healthy
- [ ] `/health/ready` 200、前端 5280 200
- [ ] 管理员初始化成功、可登录

**⏸ 确认点 2 → 你确认后进入阶段 3**

---

## 阶段 3：codex 中转站接入 + 本地执行面就绪（重点）

机制（最新 main）：Bridge 本机进程 → 六步启动门（含 **codex 就绪门**：找 codex/node → 隔离探测 → `codex app-server` 握手 → 检查 `CODEX_HOME/auth.json`）→ Matrix → readiness 上报（内存租约 45s / 15s 续租）→ 物化前逐成员门禁（未 ready 409）。

中转站接入结论（已调研）：codex 通过 `CODEX_HOME/config.toml` 的 `model_providers` 支持自定义 OpenAI 兼容端点；provider 定义**必须写在 CODEX_HOME 级 config.toml**（正是我们每个成员 codex-home 的位置）；认证走 `auth.json` 文件（我们的 Bridge 受限环境只有 6 个环境变量白名单，**不能用 env_key 环境变量方式**，必须用 auth.json）。Bridge 的 governed config 只改写自己的 managed block、保留 `model_providers` 等其余内容，不会冲突。

### 3.1 中转站连通性验证（先在"源 codex-home"验证，不污染成员）

```powershell
# 1) 确认版本
codex --version
# 2) 建一个源 codex-home 并写 config.toml（值来自阶段 0 你提供的信息）
$src = "$env:LOCALAPPDATA\repomesh-agent-bridge\sessions\relay-source\codex-home"
New-Item -ItemType Directory -Force $src | Out-Null
@"
model = "<中转站模型名>"
model_provider = "relay"
[model_providers.relay]
name = "relay"
base_url = "<中转站 base_url>"
wire_api = "chat"                # 或 responses（按中转站支持）
requires_openai_auth = true      # 认证复用 auth.json 的 OPENAI_API_KEY
"@ | Set-Content -Encoding UTF8 (Join-Path $src "config.toml")
# 3) 用中转站 key 登录（key 走 stdin，不留命令行历史）
echo "<中转站 API Key>" | codex login --with-api-key
# 4) 实测连通（源 codex-home 配置下跑一条消息）
$env:CODEX_HOME = $src
codex exec --skip-git-repo-check "用一句话介绍你自己"
```
- 若 401/404/模型不存在 → 按中转站实际调整：`wire_api` 切换 chat/responses、模型名加前缀、`requires_openai_auth` 增删。**此步通过后**才继续。

### 3.2 复制 config.toml + auth.json 到六个成员 codex-home

`copy_codex_auth.ps1` 只复制 auth.json（特意不碰 config.toml），所以 config.toml 需要单独复制（一次性，不进仓库）：

```powershell
# 复制 auth.json（官方脚本）
.\scripts\bridge-e1\copy_codex_auth.ps1 -Members scripts\bridge-e1\members.json -SourceCodexHome $src
# 复制 config.toml 到六个成员（等价路径由 e1_config.py 给出）
.\.venv\Scripts\python.exe scripts\bridge-e1\e1_config.py --members scripts\bridge-e1\members.json --codex-homes
# ↑ 输出各成员 codexHome 后，把 $src\config.toml 循环复制进每个 codexHome（在首次 start 之前完成）
```
> 必须在**首次 `start_members.ps1` 之前**完成：`prepare_session_dirs` 首次 ensure_ready 会给 codex-home 打 Low 完整性标签，之后塞不进文件。

### 3.3 平台侧开通六个 external 成员（一次性）

按 `scripts\bridge-e1\members.example.json` 写 `members.json`（worker 必须带 `workspaceRoot`，leader 不得带），依次：
```powershell
.\.venv\Scripts\python.exe scripts\bridge-e1\seed_members.py
.\.venv\Scripts\python.exe scripts\bridge-e1\provision_members.py --stage provision
# materialize 建 Team（拓扑 reconcile 自动建）
.\.venv\Scripts\python.exe scripts\bridge-e1\provision_members.py --stage binding
.\.venv\Scripts\python.exe scripts\bridge-e1\fetch_matrix_tokens.py
.\.venv\Scripts\python.exe scripts\bridge-e1\make_enrollments.py
```
成员 token 一式两份：后端 `REPOMESH_RUNNER_WORKER_TOKENS` + env 文件 `output\bridge-team\e1-members.env`（`E1_<KEY>_REPOMESH_TOKEN` / `E1_<KEY>_MATRIX_TOKEN`）。

### 3.4 启动执行面

```powershell
.\scripts\start-local-cli.ps1 -DryRun   # 先看命令
.\scripts\start-local-cli.ps1           # 实际启动
# 或生成 output\local-launcher\config.json 后，从 Console「本地 CLI」页一键启停
```

### 3.5 阶段产出与验证

- [ ] 源 codex-home 调通中转站（3.1 实测通过）
- [ ] 六个成员 codex-home 均有 auth.json + 中转站 config.toml
- [ ] Console「本地 CLI」页 6/6 ready（约 40s）
- [ ] 小需求物化成功、codex 成员消费任务并回写

**⏸ 确认点 3 → 你确认后进入阶段 4**

---

## 阶段 4：端到端验收 + 提交代码修复

### 4.1 重启恢复演练（验证同事分析的"无自愈"已解决）
```powershell
# 模拟 Docker 引擎重启/崩溃后：
# 1) 重启 Docker Desktop，等引擎就绪
# 2) docker ps -a 核对
# 3) 重跑启动命令（depends_on 只在启动时生效，干等不来）
.\scripts\start-platform.ps1
.\scripts\start-local-cli.ps1
# 4) 后端重启后租约在内存全失（fail-closed）→ Bridge 15s 内自动重新上报 → 页面变绿
# 5) readiness 6/6 后再物化
```

### 4.2 提交代码修复到仓库
- [ ] `migrations/versions/<新 merge 迁移>`（阶段 1.2）
- [ ] `compose.yaml` postgres `restart: unless-stopped`（阶段 1.3）
- （可选）`copy_codex_auth.ps1` 增加 `-ConfigToml` 复制参数，避免手工复制 config.toml

### 4.3 最终验证清单
- [ ] 迁移单 head、空库可迁移
- [ ] 平台栈全绿、可登录
- [ ] 中转站 codex 6/6 ready、物化闭环
- [ ] 重启恢复演练通过

---

## 附录 A：中转站 config.toml 参考模板（值以你提供为准）

```toml
model = "<模型名>"
model_provider = "relay"

[model_providers.relay]
name = "relay"
base_url = "<https://.../v1>"
wire_api = "chat"               # 或 "responses"
requires_openai_auth = true     # 认证走 auth.json 的 OPENAI_API_KEY
```

## 附录 B：排障速查

| 现象 | 原因 | 处理 |
|---|---|---|
| api 崩溃循环，日志 `Multiple head revisions` | 双 head 未修复 | 回到 1.2 |
| api 崩溃循环，`table already exists` / `schema does not exist` | 旧数据卷 | 回到 1.1 清空 |
| `no auth.json under CODEX_HOME` | 未登录/未复制 | 3.2 步骤重做 |
| 中转站 401/404 | wire_api 或模型名不符 | 3.1 内调整实测 |
| readiness stale/offline | Bridge 挂/网络断（>45s 无续租） | Launcher restart 或重跑 start-local-cli.ps1 |
| postgres 没起来 | 无 restart + 引擎崩 | 补 1.3 并重跑 start-platform.ps1 |
