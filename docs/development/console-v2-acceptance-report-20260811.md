# 交付控制台 v2 最终验收报告（2026-08-11）

- 文档状态：验收完成（含截图取证，见 §5）
- 验收人：检测验收1（独立验收会话，只验收不修改；本文件是该会话唯一写入物）
- 验收对象：`feat/console-v2`。**八步实走基线 = 8cb9b5c**（比验收简报基线 1393dd0 多
  两个提交：c7c899a 原型归档、8cb9b5c replay 独立聚合——#17 清理批次②③步已入库）；
  **截图套件与 B-1/2/3 GUI 复证基线 = d838a47**（v1 退役 d35f0bb + 环境窗 commit 标注
  修正后的最终形态）。两基线间提交均为前端清理与文案修正，不改变八步结论；
  工作树除用户手笔的 PRD 文档与本报告外干净
- 验收模式：**live**（`?source=live`，页脚数据源自述亲证）；replay 模式不在本次范围
- 方法论：亲自实证不采信转述；写动作前后固定 pre/post 对照（curl 留档）；只报告不修改；
  未实走路径如实列举

## 1. 环境与账号

| 项 | 值 | 验证方式 |
| --- | --- | --- |
| 前端 dev | http://127.0.0.1:5280（Vite） | 浏览器实走 |
| 后端 API | http://127.0.0.1:8100/api/v1 | curl 实调 |
| 读模型鉴权 | `Authorization: Bearer console-dev-token` | curl 200；裸调 401 |
| 身份服务 | `/auth/*`，httpOnly cookie 会话 | 登录门/登出实走 |
| 账号 | jack1（管理员，用户本人创建与登录；验收人未输入任何凭据——红线遵守） | 侧栏身份区亲证 |
| 数据库 | cons-live-pg@5533（联调种子库，只读使用） | 经读模型间接验证 |
| 活体环境 | 5432/8000/agentteams 容器未做任何操作 | — |

种子锚点（验收前 curl 亲证，与交接简报一致）：
- issue D = `fa89b322`：草稿态（第 0 轮、0 仓、phase=plan、拓扑未建团）
- issue C = `e94499f9`：paused + 1 项待决策（watch）、1 轮、billing 仓返工在途
- issue B = `9129f894-fda4-519c-89ae-68c3b8327eb5`：active + 1 项待决策（approve）、
  轮次 `b6daf7a1-8ed0-56c4-8d26-81fc022f4634`、checkout 仓候选 `c1a0b2c3d4e5…`

## 2. 八步验收清单（8/8 全过）

| # | 项 | 结果 | 证据（读到的值 / 实测行为） |
| --- | --- | --- | --- |
| 1 | 登录门与 shell | √ | 侧栏真实身份 jack1·管理员；四导航（issue/仓库/团队/智能体）+ 设置底锚；工作区切换器显「未接入」（诚实降级，非编造列表） |
| 2 | live 列表 | √ | Open **3** / Closed **1**（DOM 实读 `Open3`/`Closed1`）；三锚点全中：D「计划 v1 待物化·第 0 轮·0 仓」、C「1 项待决策」+「已暂停」徽标（独立于 Open 态，契约 §2.1 合规）、B「1 项待决策」；页脚「数据源：live · GET /issues（契约 v0.2 §2）」 |
| 3 | B 详情 | √ | 需求卡（「工程契约未接入」诚实降级）；关联芯片「repomesh-e2e-checkout · 团队就绪」；决策夹「第 1 轮 · live」；房间区 teamRoom「2 成员 · 静默」带历史条目（任务指派 16:55）、leaderDM「暂无消息」（空房间不装满） |
| 4 | 授权单真值 | √ | 五项全部与后端事实吻合：授权主体 **AGENT console-demo-org-leader**、绑定快照 **b0626c42-e9e5-401f-91ac-785ee7a44936 · IMMUTABLE**、写入范围 **repomesh-e2e-checkout（仅合并 PR #7）**、HEAD-BOUND SHA **c1a0b2c3d4e5**、有效期 **30 分钟**。派生旁证：`/console/agents` 中 role=organization_leader 的唯一 agent = `7f181c57`/console-demo-org-leader；`VITE_GOVERNANCE_AGENT_ID` 为空（乙案派生生效，非写死）。确认框未勾时「批准并授权」正确 disabled |
| 5 | 真批（唯一写动作） | √ | 权限分类器拦截一次（跨会话授权不算用户意图）→ 停点上报 → **用户本人在对话内确认后执行**。真实 click 链：勾选确认框 → 按钮激活 → 点击。toast 精确命中「治理决策已记录：READY（head-bound），merge gate 放行」；决策夹即时变「本轮无待决策事项」 |
| 6 | 批准后 curl 对照 | √ | pre：pending=1、decisions 1 条 approve（head_sha `c1a0b2c3d4e5f607…`）、merge_gate `{allowed:false, reasons:["head-bound governance decision is missing"]}`、governance_decisions=[]。post：pending=**0**、decisions=**[]**、merge_gate **`{allowed:true, reasons:[]}`**、governance_decisions 落 1 条 `ready`（decided_by=`7f181c57` 即派生主体、head_sha 全长精确绑定、decided_at 2026-08-11T21:55:30Z） |
| 7 | 抽验其余面 | √ | C 详情：paused 徽标 + 两房间 LIVE ×2（billing 返工任务 in_progress 派生，非假 presence）。C teamRoom 房间流 **5 条 = 2 message + 3 系统条目**，DOM 结构级证实：仅 message 有头像气泡（size-7 圆角 ×2），系统条目为边框标签（RUNNER/门禁）无头像——契约 §5.2 硬约束合规，且带诚实脚注「系统条目是控制台投影，非房间内真实发生」。环境窗单仓切片：门禁受阻 / 尚无变更 / PR console-demo#9 / 基线快照「未接入」，无编造数字；事件时间线已迁入环境窗（四 kind 过滤在）。三页两段式：智能体页首屏行渲染 +「探测中」占位（正确区分未探测 ≠ 未接入，契约 §4.4 三义消歧合规）→ 二段回填 **9 条「探测不可达」**，总墙钟 ≈4.4s 与并发探测吻合（串行将是 ~18s）。仓库页 4 仓各 1 驻扎团队、billing 显 1 在途任务；团队页 4 团队含成员与双房间。设置页连接健康三态分开计数「**可达 0 · 不可达 9 · 无事实 0**」+ 已知缺口清单 |
| 8 | 退出登录 | √ | 登出入口在工作区切换器下拉内；点击后回登录门（含「初始化管理员」bootstrap 链接）。验收人未输入任何凭据。备案：登出销毁服务端会话（httpOnly cookie），用户本人浏览器需重新登录（已转告） |

**A 类功能性故障：0。** 实走全程无点击失效、渲染错误、崩溃或数据不一致。

## 3. 缺陷清单（按用户 2026-08-11 标准：凡不能直接通过图形界面完成的动作与行为闭环，一律视为缺陷）

判定说明：「未接入」的诚实文案本身**不是**缺陷（它是正确的呈现方式）；缺陷是文案背后
**缺失的那个能力**。分类：**A 类 = 功能存在但坏了**；**B 类 = 闭环缺失**（界面无此能力，
需新增端点/界面）；**C 类 = 可选优化**。B 类按严重度排序。

### A 类（功能存在但坏了）

**零项。** 本次实走未发现任何 A 类缺陷。

### B 类（闭环缺失，严重度降序）

| # | 缺陷 | 现状（实证） | 缺失能力 | 修复侧 |
| --- | --- | --- | --- | --- |
| B-1 | **新建 issue 不真创建** | 弹窗提交只 toast「issue 写端点尚未接入（建 Project + Intake 立项），需求文本已保留」并留草稿（`NewIssueModal.tsx:36`） | issue 写端点（建 Project + Intake）；这是「需求进入系统」的闭环最大缺口——当前需求只能靠后台种子/脚本进入 | 后端新端点 + 前端接线 |
| B-2 | **工作区（组织）无列表、无法切换、创建只 toast** | 切换器下拉显「组织读模型未接入（CONS-32），暂无工作区列表」；「＋创建工作区」toast「写路径未接入」（`SidebarV2.tsx:129,135`） | 组织读端点（列表）+ 组织写端点（创建/绑定 Org Leader）；多工作区隔离能力界面不可用 | 后端两端点 + 前端接线 |
| B-3 | **证据面「查看证据」未接入** | 决策夹按钮点击只 toast「证据面（CI 报告 / 变更详情）未接入。门禁与变更可在房间视图的环境窗查看」（`IssueDetailContainer.tsx:131`） | 决策证据聚合视图（CI 报告、变更 diff、验证快照详情）；治理审批目前只能凭摘要行审批，证据链在 GUI 内不可达 | 后端证据端点 + 前端证据面 |
| B-4 | **issue 无关闭/归档界面动作** | 后端 `POST /deliveries/{id}/archive` 存在、前端 client 有 `archiveDelivery`（`client.ts:158`）但**无任何界面挂它**；issue 级归档实体本就未立项（契约 §6.1） | 轮次归档按钮 + issue 级关闭语义；当前归档只能 curl | 前端挂既有端点（轮次级）；issue 级待实体立项 |
| B-5 | **clarify / ChangeRequest 回路缺失** | 房间输入框已按产品决策移除（b6b04b2），代码留恢复标记；Agent 提问 → 人回答 → 回写契约的闭环无任何 GUI 入口 | ChangeRequest 完整回路（team-handoff §5.4 机制）；人对在途交付的介入手段界面上仅剩治理批准一种 | 后端机制设计 + 前端入口恢复 |
| B-6 | **跨轮次决策查看无入口** | 决策夹只显当前轮；历史轮次的治理决策 GUI 不可见（backlog 备案项） | 轮次索引下的历史决策视图 | 前端（数据已有：聚合含 governance_decisions） |
| B-7 | **issue 列表无服务端筛选** | 筛选按钮自述「仅过滤已加载的条目，不代表全量结果」；`?repository_id=`/`?phase=` 契约明确不做（§7.1 裁决撤按钮） | 服务端筛选参数；规模变大后列表不可用性风险 | 后端参数 + 契约修订 |
| B-8 | **智能体醒睡态与运行时长无源** | 花名册两列恒空（`uptime_seconds`/`awake` 恒 null，契约 §4.4）；设置页已知缺口自述 | AgentTeams Controller 需在 status 暴露启动时间戳与观测态（上游 CRD 变更） | AgentTeams 侧 |
| B-9 | **团队/房间无拆除路径** | 设计文档已知边界（DESIGN-DECISION-V2 §5）；GUI 无解散/清理动作 | 团队生命周期运维动作 | 后端 + 运维立项 |
| B-10 | **设置页 runtime 适配器配置/接入为只读** | 页面自述「适配器注册表 API 尚未立项，本页不提供配置入口」 | 适配器注册表写路径（二期） | 后端立项 |

另备案（不单列缺陷）：读模型共享动作 token 与本地登录会话是两套鉴权尚未打通
（契约 Q1 裁决维持现状 + backlog）——它不阻断任何 GUI 动作，但属于「界面已登录、
数据面却另有一把钥匙」的架构悬挂，修复阶段建议与 B-2 一并考虑。

### C 类（可选优化）

| # | 项 | 现状 |
| --- | --- | --- |
| C-1 | 授权弹窗 DOM 常驻页面（display 隐藏但出现在 a11y 树，`modalVisible:false` 实测） | 对屏幕阅读器可能可见；建议改条件渲染。不阻塞 |

## 4. 未实走路径（如实列举）

1. **replay 模式四场景**——本次验收对象是 live；replay 回归未走（8cb9b5c 刚给 replay
   独立聚合，建议合 PR 前由施工方自证 + 主脑抽验）。
2. **bootstrap 初始化管理员流程**——红线禁止验收人创建账号/输密码；仅确认登录门上
   入口存在。
3. **审批 409 / 幂等重放路径**——种子 B 的 approve 样本只有一次，未测 SHA 漂移 409
   与幂等键重放（后端 CONS-12 曾实走过，本轮未复测）。
4. **新建 issue 弹窗、工作区下拉、查看证据三处 GUI 行为**——缺陷 B-1/B-2/B-3 的现状
   描述基于代码级核实（文件:行号已注），GUI 复证与截图随 §5 截图轮补做。
5. **v1 控制台（#/delivery-v1）**——正在退役（#17 第 4 步），不验。
6. **多用户/多角色鉴权面**——只有 jack1 管理员一个账号，角色区分未验。

## 5. 截图取证（基线 d838a47 最终形态，图像存于验收会话记录）

套件在主脑「v1 整删完成」信号后对最终形态拍摄，共 11 帧，图像保存在验收会话
（检测验收1）的对话记录内；PR 阶段如需图像入库另行裁决。逐帧说明：

| # | 内容 | 关键可见证据 |
| --- | --- | --- |
| S1 | 登录后 shell + live 列表 | Open3/Closed1；B 因真批**已无待决策徽标**且按 updated_at 升至首位；C「1 项待决策」+「已暂停」；D「计划 v1 待物化」；页脚 live 数据源自述 |
| S2 | B 详情 | 决策夹「第 1 轮 · LIVE」+「**本轮无待决策事项**」（批准后终态）；需求卡「工程契约未接入」；芯片「repomesh-e2e-checkout · 团队就绪」 |
| S3/S4 | C teamRoom 房间流 + 环境窗 + 事件时间线（一帧双证） | message 条目带 RM 头像气泡、RUNNER/门禁为无头像系统标签；环境窗：门禁受阻 / 尚无变更 / CHANGESET 本仓位置 / PR console-demo#9 / 基线「未接入」；事件时间线四 kind 过滤按钮 |
| S5 | 智能体页（回填终态） | 9 个 · 9 已启用 · 1 有在途任务；运行时列「探测不可达」、时长「未接入」。「探测中」瞬态本轮未抓到帧（回填快于截图延迟），以 §2 第 7 项的 DOM 取证（`探测中`×3 占位实读）为准 |
| S6 | 仓库页 | 4 仓各 1 驻扎团队；billing「1 个进行中 issue · 1 个在途任务」；issue 短版 id 芯片 |
| S7 | 团队页 | 4 团队：成员 LD/WK 徽标、teamRoom+leaderDM 双房间、「运行时 · 探测不可达」 |
| S8 | 设置页 | 连接健康三态分开计数「可达 0 · 不可达 9 · 无事实 0」+ 已知缺口清单（另有 8cb9b5c 时点同页一帧，内容一致） |
| S9 | 工作区下拉（B-2 复证） | 「组织读模型未接入（CONS-32），暂无工作区列表」+「＋创建工作区」+ 退出登录入口 |
| S10 | 新建 issue 弹窗（B-1 复证） | 处理者「组织 Leader（未接入）」、需求文本框、创建按钮 |
| S11 | 登录门（登出后） | 用户名/密码表单 + 「首次部署？初始化管理员」bootstrap 入口 |

toast 类瞬态（存活期短于截图链路延迟）以 DOM 断言留档，均为 true：
B-1 提交 →「issue 写端点尚未接入（建 Project + Intake 立项），需求文本已保留」；
B-2 创建工作区 →「写路径未接入」；B-3 查看证据 →「证据面（CI 报告 / 变更详情）未接入」。

- **不可复现**：批准前授权单弹窗与批准动作瞬间——写动作已消耗且不可逆（种子 B
  复测需后端重置）。以 §2 第 4/5/6 项的 DOM 取证 + pre/post curl 留档为证，
  截图为批准后终态（S2）。
- 未单独取证：环境窗 sha 行的「commit 前缀」修正（d838a47）——C 仓环境窗为
  「尚无变更」空态，无 sha 行可拍；该修正为纯文案提交，未列验收项。

## 6. 总结论

**可进入合 PR 流程。** 八步清单 8/8 全过、A 类功能性故障零项、契约红线（诚实数据、
状态映射唯一实现、§5.2 系统条目、§4.4 三义消歧、head-bound 治理）实走全部合规；
唯一写动作在用户本人确认后执行且 pre/post 对照三项全中。B 类闭环缺陷 10 项为
能力缺失而非故障，按用户标准全部入册待分派修复；其中 B-1（issue 写端点）与
B-2（工作区读写）是「从界面发起需求」这条产品主动线的先决条件，建议修复阶段
最先分派。
