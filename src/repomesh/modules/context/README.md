# Context

Owns versioned context objects, visibility scopes, effective permissions, bundles, and read-only
context workspaces. It records exactly which versions and hashes a run could read. It does not
own source worktrees or secret values.
