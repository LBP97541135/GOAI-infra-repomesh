# Agent Directory

Owns long-lived logical agent identities, capabilities, concurrency limits, manager/resource
bindings, and coding-adapter registrations. It does not own running worker processes, task
scheduling, or credentials. Public contracts will be added before another module consumes it.
