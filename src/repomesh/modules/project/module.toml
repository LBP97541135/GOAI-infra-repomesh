name = "project"
owner = "project-planning"
status = "active"
schema = "project"
responsibilities = ["project lifecycle", "project repositories", "agent memberships", "repository team topology", "workstreams"]
