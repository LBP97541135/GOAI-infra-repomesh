# Workspace Adapters

Adapters create, destroy, restore, stash, and clean isolated source worktrees. A multi-repository
workspace is reserved for joint validation; a coding task receives one writable repository.
