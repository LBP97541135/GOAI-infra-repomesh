# SCM Adapters

GitHub and GitLab implementations resolve repositories, inspect branches and pull requests, push
approved branches, and expose review/conflict state. Only Delivery may authorize remote writes.
