# Coding Agent Adapters

Provider-specific manifests, CLI parsing, authentication checks, prompt delivery, hooks, native
session metadata, and restore commands live here. Process supervision, permissions, workspace
cleanup, artifact collection, and run state remain owned by Agent Runtime.
