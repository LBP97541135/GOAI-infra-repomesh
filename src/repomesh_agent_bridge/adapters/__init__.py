"""Concrete sides of the Bridge's seams.

``repomesh_binding`` is the production adapter for :class:`WorkerBindingPort`;
``memory`` holds the in-memory and inert stand-ins that PR 2 actually runs with,
since Matrix and the coding CLI do not arrive until PR 3 and PR 4.
"""
