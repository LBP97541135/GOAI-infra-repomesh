# 需求文档：通知摘要 —— 邮件与站内信合并为每日一封

> 本文档由数据集中第一条 issue 生成，供测试使用。需求原文、issue 元信息均来自系统读模型，功能展开为需求分析产物。

## 0. 来源与元信息

| 项目 | 值 |
| --- | --- |
| issue_id | `fa89b322-6867-5422-95ea-07dbb8127713` |
| 需求原文（逐字） | 通知摘要：邮件与站内信合并为每日一封 |
| 当前阶段 | plan（计划 v1 待物化） |
| 所属工作区 | `d68a5926-824c-55cd-b1fd-56d2c8d3b760` |
| 提出主体 | `console-demo-org-leader` |
| 创建时间 | 2026-08-13T07:53:10Z |
| 发现链状态 | step 1 / idle（未发起分析） |

## 1. 需求背景

用户每天会同时收到两类通知：邮件与站内信。两类通知由不同事件触发、各自独立推送，同一件事（例如一条待办审批、一次交付状态变化）常常在邮件和站内信里各出现一次，用户需要打开两个入口才能把一件事看全。通知越分散，被忽略的概率越高——邮件堆积在收件箱，站内信沉底在消息中心，用户既不关闭通知，也不处理通知，通知系统的价值被稀释。

需求原文要求把两类通知合并为**每日一封**摘要：每天一次、一个入口、把当天发生的所有事情讲清楚。合并的落点是「少打扰」而非「少通知」——通知总数不减少，但触达次数从「每事件一次」收敛为「每天一次」。

## 2. 需求目标

- 用户每天只收到一封摘要通知，替代当天分散的邮件与站内信逐条推送。
- 摘要在一处承载当天全部事件，用户不必在邮件与站内信两个入口之间来回。
- 摘要保留逐条通知的可操作性：每条事件仍可展开、可跳转、可处理。

## 3. 功能需求详情

### 3.1 摘要聚合范围与规则

**触发**：摘要每日生成一次，聚合从上一封摘要发出后到本封摘要生成前发生的事件。

**处理规则**：

- 聚合对象为当日产生的全部通知事件，覆盖邮件渠道与站内信渠道的两类来源，不因渠道不同而重复保留。
- 同一事件同时落入两个渠道时按事件去重，只保留一条，并在摘要中标注该事件支持的处理渠道。
- 事件按类型分组呈现，分组维度为事件类别（审批、交付、评论、系统告警等），组内按发生时间倒序排列。
- 已被用户处理过的事件（例如已批准的审批、已读的评论）在摘要中保留，但标记为已处理，不再作为待办项计数。
- 摘要生成失败时，未纳入摘要的事件顺延并入下一封摘要，不丢弃、不重复计数。

**输出**：一封摘要，包含事件总数、待办计数、分组事件清单。

**状态变化**：摘要生成完成后，本次窗口关闭，下一窗口重新计时。

### 3.2 每日摘要的生成与送达

**触发**：每日固定时间点触发一次，触发时间可由工作区配置，默认每日 09:00。

**处理规则**：

- 摘要按邮件与站内信两个渠道分别送达，两个渠道的摘要内容一致，仅在排版上适配渠道。
- 摘要送达使用既有通知通道，不新建通道；通道不可用时按通道既有重试策略处理，重试仍失败则仅在该渠道降级（不影响另一渠道送达）。
- 当日无任何事件时仍生成一封空摘要，正文说明「今天没有需要处理的通知」，避免用户因缺失一封邮件而怀疑系统停摆。
- 摘要的送达动作记入通知记录，可查询、可追溯。

**输出**：邮件渠道一封邮件，站内信渠道一条消息，两者内容同源。

**状态变化**：送达成功 → 该封摘要标记已发送；送达失败 → 进入重试，最终失败则记录失败原因。

### 3.3 摘要内容结构

**触发**：用户打开摘要。

**处理规则**：

- 主题行包含日期与待办计数，例如「8月13日通知摘要 · 3 项待处理」。
- 正文顶部为概览区：事件总数、待办数、已处理数。
- 概览区之下按事件分组，每组标题为该组类别名，组内逐条列出事件。
- 单条事件展示：事件标题、发生时间、所属对象（如关联的 issue、仓库）、状态；可点击跳转到事件详情或原文位置。
- 站内信渠道的摘要支持「全部标为已读」操作，邮件渠道不提供该操作。

**输出**：结构化摘要正文。

**状态变化**：用户阅读并处理事件后，事件状态更新，下一封摘要中该事件显示为已处理。

### 3.4 摘要偏好设置

**触发**：用户在工作区设置中调整通知偏好。

**处理规则**：

- 用户可开启或关闭「每日摘要」；关闭后恢复为事件逐条即时通知。
- 用户可选择摘要的送达渠道：仅邮件、仅站内信、两者都要，默认两者都要。
- 用户可修改每日摘要的生成时间，修改后从下一个生成窗口生效。
- 偏好设置仅影响该用户本人，不影响工作区其他成员。

**输出**：偏好保存成功提示；偏好即时生效。

**状态变化**：保存偏好 → 偏好更新；关闭摘要 → 下一窗口起按逐条通知送达。

### 3.5 站内信阅读中心与未读状态

**触发**：用户在消息中心打开摘要或单条事件。

**处理规则**：

- 站内信渠道的通知统一收进消息中心，摘要与逐条通知同列表展示，摘要条目标注「摘要」徽标。
- 每条通知维护未读/已读状态；打开详情即标记已读，也可批量标为已读。
- 未读数在消息中心入口显示角标，未读数不受摘要合并影响——合并改变的是送达节奏，不改变未读计数口径。

**输出**：消息列表、未读角标、已读标记。

**状态变化**：打开 → 未读变已读；批量操作 → 选中项全部已读。

## 4. 字段与状态一览

### 4.1 摘要字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| digest_id | 标识 | 是 | 摘要唯一标识 |
| window_start / window_end | 时间 | 是 | 本封摘要聚合的事件时间窗口 |
| event_total | 计数 | 是 | 事件总数 |
| pending_count | 计数 | 是 | 待办计数 |
| resolved_count | 计数 | 是 | 已处理计数 |
| groups | 分组 | 是 | 按事件类别的分组清单 |
| channels | 枚举 | 是 | 送达渠道：email / inapp / both |
| status | 枚举 | 是 | 待发送 / 已发送 / 失败 |

### 4.2 通知事件状态

| 当前状态 | 允许操作 | 下一状态 | 触发条件 |
| --- | --- | --- | --- |
| 未读 | 打开、标已读 | 已读 | 用户打开详情或批量标已读 |
| 未读 | 处理（如审批） | 已处理 | 用户在事件详情完成操作 |
| 已读 | 处理（如审批） | 已处理 | 用户在事件详情完成操作 |
| 已处理 | 无 | 已处理 | 终态 |

## 5. 测试用法

本需求文档配套测试输入使用，两种玩法：

**玩法 A：在控制台新建 issue 走发现链**。打开 front（http://127.0.0.1:5280/），新建 issue，把下面这段需求文本粘贴进需求输入框，发起发现链，即可观察 Step 0 需求分析 → Step 1 候选评分 → Step 2 三档分类 → Step 3 生成计划的完整流程：

```
通知摘要：邮件与站内信合并为每日一封。要求用户每天只收到一封摘要通知，
聚合当天全部事件（审批、交付、评论、告警），按事件类型分组展示；
同一事件在两个渠道同时出现时去重；已处理事件保留但标注已处理；
支持用户关闭摘要恢复逐条通知、选择送达渠道、修改每日生成时间；
当日无事件时仍发送空摘要说明当天无事。
```

**玩法 B：回放既有 issue**。在 8100 读模型上直接查看本 issue 的发现链投影（`GET /api/v1/issues/fa89b322-6867-5422-95ea-07dbb8127713/discovery`），当前处于 step 1 / idle，可作为「从未发起发现的 issue」的对照样本，验证空态渲染与步进器状态。
