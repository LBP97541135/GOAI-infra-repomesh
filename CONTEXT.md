# RepoMesh

多仓库 AI 编制平台：把一个组织的仓库交给分层的 Agent 团队（Manager → 各团队）去规划、
施工与验证。本表只收本项目特有的领域语言，不收实现细节。

## Language

### 编制与拓扑

**Manager**：
一个组织唯一的编排 Agent（organization leader），冻结判据、向各团队队长派工、承接升级。
_Avoid_: 主 Agent（规程文档用语，平台侧一律称 Manager）、总管

**仓库团队**：
锚定在一个仓库上的 Agent 编制单元：一名队长加至少一名 worker，共享该仓的两间房。
_Avoid_: 小组、component team

**materialize（开工）**：
把已批准的计划变成活的编制与任务的动作：确保拓扑、投运行时、铸任务，可重入。
_Avoid_: 部署、启动项目

### 跨仓联调（测试团队线）

**测试团队**：
锚定在测试资产仓上的一支仓库团队，不是独立的团队类型；专职跨仓联调，不做单仓测试。
_Avoid_: TestTeam（一等公民类型，已拒）、QA 团队

**测试资产仓**：
测试团队自有的 git 仓：场景库、环境定义、联调证据的唯一归宿，也是正式测试资产的入库地。
_Avoid_: 测试目录、evidence 仓

**档案开关**：
catalog 仓库行上的 capability profile 充当的供给侧开关：只决定新拓扑建不建测试团队，
对存量已建团队无追溯力。
_Avoid_: 运行时开关、kill switch

**联调轮**：
对一个钉死组合在一次性隔离环境里的一次完整执行，以 run-id 标识；台账以任务为准。
_Avoid_: 测试批次、round trip

**组合**：
仓库到钉死 commit 的映射表，联调轮的被测对象；由 Manager 冻结，团队不得自行改钉。
_Avoid_: 版本集、快照

**判据**：
Manager 随任务下发并冻结的验收依据：module-test spec 加组合；红灯优先判业务 Bug 而非改判据。
_Avoid_: 验收标准（那是给交付物的，判据是给联调轮的）

**最小证据集**：
每场景四元组：命令、退出码、输出摘录、request-id。摘录有上限，全量原始日志不属于它。
_Avoid_: 全量日志、执行记录

**轮次目录**：
测试资产仓里一个联调轮的证据提交（`evidence/<run-id>/`）；BLOCKED 轮同样要有。
_Avoid_: 日志目录

**一次性隔离环境**：
为一个联调轮新建、参数化命名空间、测完整体拆除的工作现场；不存在常驻联调环境。
_Avoid_: 沙箱（太泛）、staging
